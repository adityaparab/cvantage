export const SCHEMA_PROMPT = `Design a reusable JSON Schema draft-07 for ALL non-PII resume facts. Extend latestSchema, preserving every existing field and type. Use only type, title, properties, required, additionalProperties:false, items, and root $schema. Types: object, array, string, number, boolean. Objects require explicit properties and additionalProperties:false. Required root sections: basics (professional info object), professionalSummary (string), workExperience (array), skills (array of {category:string,items:string[]}). Include additional sections evidenced in source. No name, email, phone, contactNumber, location, examples, defaults or personal values. Field keys are camelCase ASCII. Maximum depth 8 and 300 fields. Reuse latestSchema unchanged when sufficient. Return the schema itself.`;
export const MAPPING_PROMPT = `Map ALL non-PII source facts into schema. Return the resume object itself. Do not infer missing qualifications, dates, employers, skills or achievements. Use empty strings/arrays/objects only where valid. Retain all relevant details and additional sections. Ignore [REDACTED] tokens. Feedback describes previous problems to correct.`;
export const JUDGE_PROMPT = `Evaluate candidate independently against source, schema and stage. For schema verify all source facts can be represented, required sections/types, compatibility with latestSchema, no speculative fields or personal values. For mapping verify schema conformance, completeness, source fidelity, no invented facts and no PII. Return exactly {stage:"schema"|"mapping",verdict:"accept"|"revise",confidence:number 0..1,checks:{structureValid:boolean,sourceCovered:boolean,sourceFaithful:boolean,piiAbsent:boolean},issues:[{code:string,path:JSON Pointer or empty,message:string,suggestedFix:string}]}. Accept only if all checks true, confidence >=0.90 and issues empty. Confidence .70..<.90 indicates unresolved uncertainty, below .70 substantial uncertainty. Never copy source values into issues; refer to field paths and explain corrections generically.`;
