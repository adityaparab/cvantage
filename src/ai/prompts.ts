export const SCHEMA_PROMPT = `Inspect source for fields that latestSchema cannot already represent. The repository schema is the immutable baseline. Return exactly {"additions":[{"parentPath":"", "name":"newField", "definition":{"type":"string"}, "evidence":"exact contiguous excerpt from source"}]}. Empty additions means reuse latestSchema unchanged. parentPath is a JSON Pointer to an existing object schema, e.g. "" for the root or "/properties/work/items" for a work entry. Only add absent properties. Never remove, rename, replace or modify existing fields, required lists, metadata, types or constraints. Never return a full schema. Use existing basics.summary, work, skills[].name/level/keywords and other base sections wherever they already fit. New skill categories are values in skills[].name, not new schema fields. Avoid synonyms or duplicate aliases for existing fields. Every addition needs a source excerpt demonstrating genuinely new non-PII information; the judge must confirm it is not already representable. Field definitions use only type, title, properties, required, additionalProperties:false, and items; types object/array/string/number/boolean. No personal values, identifying fields, examples, defaults, refs or executable keywords in additions. New field names must be camelCase ASCII. Do not add fields for missing information. Maximum 30 additions, depth 8 and 500 total fields. PII_NAME, PII_EMAIL, PII_PHONE, PII_LOCATION and legacy [REDACTED] tokens are placeholders, not resume facts or new fields.`;
export const MAPPING_PROMPT = `Map ALL non-PII source facts into schema. Return the resume object itself. Follow existing field names exactly: professional summary belongs in basics.summary, employment in work, and skill categories in skills[].name with keywords. Historical schemas may also contain professionalSummary/workExperience or skills[].category/items; honor their recorded fields and required constraints. Omit basics.name/email/phone/location and root $schema/meta entirely: contact values are stored separately and tooling metadata is not resume content. Never copy schema descriptions or examples as source facts. Omit absent optional fields, especially dates/URLs; do not invent empty values that violate formats or patterns. Do not invent undeclared keys. Do not infer missing qualifications, dates, employers, skills or achievements. Use empty strings/arrays/objects only where valid. Retain all relevant details and additional sections. PII_NAME, PII_EMAIL, PII_PHONE, PII_LOCATION and legacy [REDACTED] tokens stand for removed personal details; omit them and never infer their original values. Feedback describes previous problems to correct.`;
export const JUDGE_PROMPT = `Evaluate candidate independently against source, schema and stage. For schema verify only genuinely missing fields were added to latestSchema, existing definitions/constraints are unchanged, additions are directly evidenced by source, and source facts cannot already fit existing fields. Skill category names are data values, not schema extensions. Contact field definitions in the trusted baseline are allowed, but personal values in additions or data are not. For mapping verify schema conformance, completeness, source fidelity, no invented facts and no PII. Return exactly {stage:"schema"|"mapping",verdict:"accept"|"revise",confidence:number 0..1,checks:{structureValid:boolean,sourceCovered:boolean,sourceFaithful:boolean,piiAbsent:boolean},issues:[{code:string,path:JSON Pointer or empty,message:string,suggestedFix:string}]}. Accept only if all checks true, confidence >=0.90 and issues empty. Confidence .70..<.90 indicates unresolved uncertainty, below .70 substantial uncertainty. Ignore PII_NAME, PII_EMAIL, PII_PHONE, PII_LOCATION and legacy [REDACTED] tokens; they are removed personal details, not missing facts. Never copy source values into issues; refer to field paths and explain corrections generically.`;
